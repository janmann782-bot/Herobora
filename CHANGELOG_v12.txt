INFOBOX BOT v12

Исправлено чередование строк в теме Aurelia.

Раньше полосы считались отдельно внутри каждого раздела. Из-за этого первая строка каждого нового раздела снова была чёрной, и при коротких разделах подсвечивалась только одна редкая строка.

Теперь счёт идёт сквозь всю карточку:
1 поле — чёрное
2 поле — #132A18
3 поле — чёрное
4 поле — #132A18
и так далее независимо от заголовков разделов.

Исправлено в обоих рендерах:
- HTML/CSS + Chromium
- Pillow fallback

Логирование карточки в группу -1004368904200 на этапе рендера из v11 сохранено.

Проверка:
- python -m py_compile *.py
- pytest -q test_core.py -> 23 passed
